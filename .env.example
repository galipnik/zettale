# Copy to .env and fill in. Never commit the real .env.
DOMAIN=todo.yourserver.com
TODO_TOKEN=replace-with-a-long-random-secret
# Name of the existing Nginx Proxy Manager docker network.
# Check with: docker network ls   (often <npm-project>_default, e.g. nginx_default)
NPM_NETWORK=nginx_default
